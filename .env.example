# Template for .env.local — copy to .env.local and fill in real values.
# .env.local is git-ignored; this .example file is safe to commit (no secrets).

KLAVIYO_PRIVATE_API_KEY=pk_your_private_key_here
KLAVIYO_API_REVISION=2024-10-15
